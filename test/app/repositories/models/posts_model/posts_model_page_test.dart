import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:desafio/app/repositories/models/posts_model/posts_model_page.dart';

main() {
  testWidgets('PostsModelPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PostsModelPage(title: 'PostsModel')));
    //  final titleFinder = find.text('PostsModel');
    //  expect(titleFinder, findsOneWidget);
  });
}
