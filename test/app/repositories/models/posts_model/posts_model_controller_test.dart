import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:desafio/app/repositories/models/posts_model/posts_model_controller.dart';
import 'package:desafio/app/app_module.dart';

void main() {
  initModule(AppModule());
  // PostsModelController postsmodel;
  //
  setUp(() {
    //     postsmodel = AppModule.to.get<PostsModelController>();
  });

  group('PostsModelController Test', () {
    //   test("First Test", () {
    //     expect(postsmodel, isInstanceOf<PostsModelController>());
    //   });

    //   test("Set Value", () {
    //     expect(postsmodel.value, equals(0));
    //     postsmodel.increment();
    //     expect(postsmodel.value, equals(1));
    //   });
  });
}
