import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:desafio/app/repositories/models/post_model.dart';
import 'package:desafio/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ModelsController models;
  //
  setUp(() {
    //     models = AppModule.to.get<ModelsController>();
  });

  group('ModelsController Test', () {
    //   test("First Test", () {
    //     expect(models, isInstanceOf<ModelsController>());
    //   });

    //   test("Set Value", () {
    //     expect(models.value, equals(0));
    //     models.increment();
    //     expect(models.value, equals(1));
    //   });
  });
}
