import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:desafio/app/repositories/models/models_page.dart';

main() {
  testWidgets('ModelsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ModelsPage(title: 'Models')));
    //  final titleFinder = find.text('Models');
    //  expect(titleFinder, findsOneWidget);
  });
}
